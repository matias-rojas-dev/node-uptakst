doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
    body 
        h2 Confirmar cuenta
        p Click en el siguiente enlace para confirmar tu cuenta. Este enlace es temporal 
        a(href=`${confirmUrl}`) Confirmar cuenta
        p Si no puedes acceder, visita #{confirmUrl}
        p Si no solicitaste este email, ignora esto